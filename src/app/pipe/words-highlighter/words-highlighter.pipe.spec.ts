import { WordsHighlighterPipe } from './words-highlighter.pipe';

describe('WordsHighlighterPipe', () => {
  it('create an instance', () => {
    const pipe = new WordsHighlighterPipe();
    expect(pipe).toBeTruthy();
  });
});
